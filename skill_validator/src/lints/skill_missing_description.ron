SkillLint(
    id: "skill_missing_description",
    human_readable_name: "SKILL.md frontmatter missing description",
    description: "A SKILL.md file's frontmatter is missing the required `description` field.",
    lint_level: Deny,
    reference_link: Some("https://agentskills.io/specification#description-field"),
    query: r#"
    {
        Skill {
            has_frontmatter @filter(op: "=", value: ["$true"])
            description @filter(op: "is_null")

            skill_file_path @output
            group_folder @output
            folder_name @output

            span_: frontmatter_end_span @optional {
                filename @output
                begin_line @output
                end_line @output
            }
        }
    }"#,
    arguments: {
        "true": true,
    },
    error_message: "A SKILL.md file's frontmatter is missing the required `description` field.",
    per_result_error_template: Some("{{skill_file_path}} frontmatter is missing required field: description"),
)
